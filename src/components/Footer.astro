---
const currentYear = new Date().getFullYear();

const footerLinks = {
  product: [
    { name: "Features", href: "/#features" },
    { name: "Pricing", href: "/pricing" },
    { name: "FAQ", href: "/#faq" },
  ],
  company: [
    { name: "About Us", href: "/about" },
    { name: "Blog", href: "/blog" },
    { name: "Contact", href: "/contact" },
  ],
  legal: [
    { name: "Privacy Policy", href: "/privacy-policy" },
    { name: "Terms & Conditions", href: "/terms-and-conditions" },
    { name: "Data Policy", href: "/data-policy" },
  ],
};

const socialLinks = [
  { name: "Facebook", icon: "fab fa-facebook", href: "#" },
  { name: "Twitter", icon: "fab fa-twitter", href: "#" },
  { name: "LinkedIn", icon: "fab fa-linkedin", href: "#" },
  { name: "Instagram", icon: "fab fa-instagram", href: "#" },
];
---

<footer class="bg-white border-t border-border-light pt-16 pb-8">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-12 mb-16">
      <!-- Brand Section -->
      <div class="lg:col-span-2">
        <div class="flex items-center space-x-2 mb-6">
          <div
            class="w-10 h-10 bg-primary-400 rounded-xl flex items-center justify-center shadow-lg shadow-primary-400/20"
          >
            <i class="fas fa-wallet text-white text-xl"></i>
          </div>
          <div class="flex flex-col">
            <span class="font-bold text-xl text-text-primary tracking-tight"
              >Spentiva</span
            >
            <span
              class="text-[10px] uppercase tracking-wider text-text-secondary font-medium"
              >By Exyconn</span
            >
          </div>
        </div>
        <p class="text-text-secondary mb-8 max-w-sm leading-relaxed">
          AI-powered expense tracking made simple. Track, analyze, and optimize
          your spending with intelligent insights.
        </p>
        <div class="flex space-x-4">
          {
            socialLinks.map((social) => (
              <a
                href={social.href}
                class="w-10 h-10 bg-background-surfaceAlt rounded-lg flex items-center justify-center text-text-secondary hover:bg-primary-400 hover:text-white transition-all duration-300 transform hover:-translate-y-1"
                aria-label={social.name}
              >
                <i class={`${social.icon} text-lg`} />
              </a>
            ))
          }
        </div>
      </div>

      <!-- Product Links -->
      <div>
        <h3 class="font-bold text-text-primary mb-6">Product</h3>
        <ul class="space-y-4">
          {
            footerLinks.product.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-text-secondary hover:text-primary-400 transition-colors"
                >
                  {link.name}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- Company Links -->
      <div>
        <h3 class="font-bold text-text-primary mb-6">Company</h3>
        <ul class="space-y-4">
          {
            footerLinks.company.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-text-secondary hover:text-primary-400 transition-colors"
                >
                  {link.name}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- Legal Links -->
      <div>
        <h3 class="font-bold text-text-primary mb-6">Legal</h3>
        <ul class="space-y-4">
          {
            footerLinks.legal.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-text-secondary hover:text-primary-400 transition-colors"
                >
                  {link.name}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="pt-8 border-t border-border-light">
      <div
        class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0"
      >
        <p class="text-sm text-text-placeholder">
          © {currentYear} Spentiva by Exyconn. All rights reserved.
        </p>
        <div class="flex items-center space-x-8 text-sm text-text-secondary">
          <a
            href="/privacy-policy"
            class="hover:text-primary-400 transition-colors">Privacy</a
          >
          <a
            href="/terms-and-conditions"
            class="hover:text-primary-400 transition-colors">Terms</a
          >
          <a
            href="/data-policy"
            class="hover:text-primary-400 transition-colors">Data Policy</a
          >
        </div>
      </div>
    </div>
  </div>
</footer>
```
